# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Jetbains
.idea/

# Built packages
cmd/pack/
cmd/pack.zip

# Local runtime config (may contain real server addresses) and logs
config.yaml
pingtunnel.stderr
pingtunnel_*.log
